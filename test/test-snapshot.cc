#include <string>

#include "base/all.h"
#include "memdb/snapshot.h"

using namespace base;
using namespace mdb;
using namespace std;

TEST(snapshot, basic_op) {
    snapshot_multimap<int, string> data;
}
